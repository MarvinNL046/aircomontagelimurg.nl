{
  "name": "aircomontagelimburg",
  "version": "1.0.0",
  "description": "Airco Montage Limburg - Professional airco installation website",
  "main": "index.html",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:netlify": "vite build && cp -r js dist/ && cp -r css dist/ && cp sitemap.xml dist/ && cp robots.txt dist/ && cp favicon.ico dist/ 2>/dev/null || true",
    "preview": "vite preview",
    "start": "npm run dev",
    "dev:live": "live-server . --port=8847 --host=localhost",
    "serve": "python3 -m http.server 8847"
  },
  "keywords": [
    "airco",
    "limburg",
    "installatie",
    "onderhoud"
  ],
  "author": "Airco Montage Limburg",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "terser": "^5.24.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "@emailjs/browser": "^4.4.1",
    "@types/node": "^24.0.15",
    "react-hot-toast": "^2.5.2",
    "typescript": "^5.8.3"
  }
}
